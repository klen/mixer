language: python

python:
  - "2.6"

env:
  - TOXENV=py26-d14,py26-d15
  - TOXENV=py27-d14,py27-d15
  - TOXENV=py33-d15

# command to install dependencies
install:
    - pip install tox

# command to run tests
script:
    - tox
